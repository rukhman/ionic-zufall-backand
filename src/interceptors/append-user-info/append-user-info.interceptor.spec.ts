import { AppendUserInfoInterceptor } from './append-user-info.interceptor';

describe('AppendUserInfoInterceptor', () => {
  it('should be defined', () => {
    expect(new AppendUserInfoInterceptor()).toBeDefined();
  });
});
